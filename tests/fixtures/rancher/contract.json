{
  "contract_version": 2,
  "source": "Rancher /v3 response shape captured read-only 2026-07-16; canonical binding and readiness fields are synthetic and aligned with kubetee-platform PR #35",
  "collection": {
    "type": "collection",
    "pagination_fields": ["first", "next", "last", "limit", "total", "partial"],
    "next_page": "follow pagination.next URL (marker-based) until the field is absent",
    "incomplete_signals": [
      "pagination.partial is true and pagination.next is absent",
      "pagination object missing on a collection",
      "unrecognized pagination fields beyond the listed set"
    ],
    "server_max_limit": 10000
  },
  "cluster": {
    "state_field": "state",
    "active_value": "active",
    "transitioning_field": "transitioning",
    "transitioning_ok_value": "no",
    "identity_fields": ["id", "uuid"],
    "label_container": "labels",
    "miner_label_key": "kubetee.ai/hotkey",
    "required_binding_labels": [
      "kubetee.ai/binding-id",
      "kubetee.ai/hotkey",
      "kubetee.ai/coldkey",
      "kubetee.ai/provider-id",
      "kubetee.ai/binding-status",
      "kubetee.ai/generation",
      "kubetee.ai/netuid",
      "kubetee.ai/network",
      "kubetee.ai/origin-fp-prefix"
    ],
    "enrollment_uid_annotation": "kubetee.ai/enrollment-uid",
    "enrolled_value": "ENROLLED (onboarding complete; not an eligibility verdict)",
    "ready_condition": {"type": "Ready", "status": "True"},
    "delete_authorization_signal": "links.remove (presence is an inference only, never proof)",
    "conditional_delete": "unsupported: norman v3 objects expose uuid but no resourceVersion; the pre-delete recheck is a GET re-validating id, uuid, label value, and non-management identity",
    "management_cluster_markers": ["internal flag", "id c-x* naming for imported management", "absence of kubetee.ai labels"],
    "secret_bearing_fields_dropped": ["caCert", "clusterSecrets", "all annotations except kubetee.ai/enrollment-uid", "appliedSpec", "importedConfig"]
  },
  "node": {
    "state_field": "state",
    "active_value": "active",
    "transitioning_field": "transitioning",
    "transitioning_ok_value": "no",
    "identity_fields": ["id", "uuid", "clusterId"],
    "ready_condition": {"type": "Ready", "status": "True"},
    "pressure_conditions": ["MemoryPressure", "DiskPressure", "PIDPressure", "NetworkUnavailable"],
    "role_fields": ["etcd", "controlPlane", "worker", "unschedulable"],
    "resource_fields": ["cpu", "memory", "nvidia.com/gpu"],
    "gpu_label_fields": ["nvidia.com/gpu.product", "nvidia.com/gpu.workload.config"],
    "runtime_handlers_field": "runtimeHandlers",
    "runtime_handler": "kata-qemu-nvidia-gpu-tdx",
    "secret_bearing_fields_dropped": ["ipAddress", "hostname", "requestedHostname", "customConfig", "annotations", "info"]
  },
  "observed_states": {
    "cluster": ["active"],
    "node": ["active"]
  },
  "transitional_or_unknown_treated_inactive": [
    "provisioning", "updating", "upgrading", "registering", "reconciling",
    "unavailable", "error", "cordoned", "draining", "waiting", "removing"
  ],
  "http_client_requirements": {
    "user_agent_required": true,
    "user_agent_example": "kubetee-validator/0.1",
    "reason": "Cloudflare fronts staging Rancher and blocks the default python-urllib signature with 'error code: 1010'; curl and custom UAs verified passing (HTTP 200)"
  }
}
