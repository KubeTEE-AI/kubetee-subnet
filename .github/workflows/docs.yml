name: Docs

# Thin caller for the KubeTEE docs subsystem. Logic lives once in
# KubeTEE-AI/kubetee-ci .github/workflows/docs-reusable.yml (no per-repo drift).

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:


jobs:
  docs:
    uses: KubeTEE-AI/kubetee-ci/.github/workflows/docs-reusable.yml@main
    secrets: inherit
