rich>=13
pytest>=8
numpy>=1
setuptools>=68
# Bittensor v11 unified package (SDK + btcli), pinned to the verified CLI contract.
bittensor==11.0.0
# For local dev / compose images
docker>=7.0.0

prometheus-client>=0.20
